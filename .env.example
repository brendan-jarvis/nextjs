# Clerk Authentication (get keys from https://dashboard.clerk.com)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Supabase / Postgres (use direct non-pooling URL for best results with Drizzle)
# SUPABASE_URL=...
# SUPABASE_ANON_KEY=...
# DATABASE_URL=postgresql://... (or the POSTGRES_* variants injected by Vercel integration)

# SECURITY: Never commit real secrets. .env and .env*.local are gitignored.
# All DB access is server-side only via Drizzle. See README and supabase-rls-policies.sql.

# Optional: skip env validation during build/CI
# SKIP_ENV_VALIDATION=1
